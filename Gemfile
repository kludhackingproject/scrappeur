source "https://rubygems.org"
ruby '2.5.1'
# rubocop is used to verify the cleaness of the code
gem 'rubocop', '~> 0.57.2'
# rspec is used to write tests
gem 'rspec'
# dotenv is used to register API keys
gem 'dotenv'
# pry i used for debuging
gem 'pry'
